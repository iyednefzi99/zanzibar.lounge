apiVersion: v1
kind: Service
metadata:
  name: e-coffee-service
  namespace: e-coffee-node
  labels:
    app.kubernetes.io/name: e-coffee-node
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: e-coffee-node
